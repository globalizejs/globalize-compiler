var compile = require( "./src/compile" );
var compileExtracts = require( "./src/compile-extracts" );
var extract = require( "./src/extract" );

module.exports = {
	compile: compile,
	compileExtracts: compileExtracts,
	extract: extract
};
